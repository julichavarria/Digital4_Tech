<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ('../partials/head') %>
        <!-- /////// CARGA CSS  PARTICULAR/////// -->
        <link rel="stylesheet" href="/css/styles.css">
        <link rel="stylesheet" href="/css/stylesRegister.css">
</head>

<body>
    <%- include ('../partials/header') %>
        <main class="mainRegister">
            <h1><i class="fa-solid fa-pen"></i></h1>
            <h1> EDITAR PRODUCTO</h1>
            <h3>Completa la información del producto a editar</h3>


            <div class="controlGeneral">
                <form action="/products/editProduct/<%= productos.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <div class="campoFormulario">
                        <label> Nombre de la PC </label>
                        <input required="required" type="text" id="nombre" name="nombre" placeholder="NOMBRE"
                            class="controlFormulario" value="<%= productos.titulo %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Procesador </label>
                        <input required="required" type="text" id="procesador" name="procesador"
                            placeholder="PROCESADOR" class="controlFormulario" value="<%= productos.procesador %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Motherboard </label>
                        <input required="required" type="text" id="mother" name="mother" placeholder="MOTHER"
                            class="controlFormulario" value="<%= productos.mother %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Placa de video </label>
                        <input required="required" type="text" id="video" name="video" placeholder="PLACA DE VIDEO"
                            class="controlFormulario" value="<%= productos.video %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Memoria Ram </label>
                        <input required="required" type="text" id="ram" name="ram" placeholder="MEMORIA RAM"
                            class="controlFormulario" value="<%= productos.ram %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Disco Rígido </label>
                        <input required="required" type="text" id="disco" name="disco" placeholder="DISCO RIGIDO"
                            class="controlFormulario" value="<%= productos.disco %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Gabinete </label>
                        <input required="required" type="text" id="gabinete" name="gabinete" placeholder="GABINETE"
                            class="controlFormulario" value="<%= productos.gabinete %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Fuente </label>
                        <input required="required" type="text" id="fuente" name="fuente" placeholder="FUENTE"
                            class="controlFormulario" value="<%= productos.fuente %>">
                    </div>
                    <div class="campoFormulario">
                        <label> Precio </label>
                        <input required="required" type="text" id="precio" name="precio" placeholder="PRECIO"
                            class="controlFormulario" value="<%= productos.precio %>">
                    </div>

                    <div class="contenedorImgEdit">
                        <div>
                            <img src=<%= destinationFolder + productos.imagen %> alt="imagen de equipo">
                        </div>
                        <div>
                            <p><i class="fas fa-angle-left"></i>Imagen Actual</p>
                            <label for="foto"><i class="fa-solid fa-camera"></i>Seleccioná la fotografía del producto
                            </label>
                            <input type="file" id="foto" name="foto" accept=".jpg" class="controlFormulario">
                        </div>
                    </div>


                    <div class="campoFormulario">
                        <label>Seleccioná la categoría de procesador</label>
                        <div class="contenedorRadioButtonsLogos">
                            <label><input type="radio" required="required" id="categoria" name="categoria" value="Intel"
                                    <% if (productos.categorias.marca =="Intel" ){%> checked <%}%>> <img
                                        src="/img/logo_intel.svg"></input></label>
                            <label><input type="radio" required="required" id="categoria" name="categoria" value="Amd"
                                    <% if (productos.categorias.marca=="Amd" ){%> checked <%}%>> <img
                                        src="/img/logo_amd.svg"></input></label>
                        </div>
                    </div>


                    <div class="botones">
                        <button class="btnPrincipal" type="submit"> GUARDAR CAMBIOS </button>
                    </div>
                </form>
                <form action="/products/editProduct/<%= productos.id %>/deleteProduct?_method=DELETE" method="POST">
                    <div class="botones">
                        <button class="btnSecundario" type="submit"><span>BORRAR PRODUCTO</span> </button>
                    </div>
                </form>
            </div>
            <p class="btnVolver"><i class="fas fa-angle-left"></i><a href="/index"> Volver a la página principal sin
                    hacer cambios</a> </p>


        </main>
        <%- include ('../partials/footer') %>
</body>

</html>